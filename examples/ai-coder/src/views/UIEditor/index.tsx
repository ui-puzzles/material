import { UIEditor } from '@ui-puzzles/editor';

export const UIEditorPreview = () => {
  return (
    <div
      style={{
        height: '100%',
        width: '100%',
      }}
    >
      <UIEditor />
    </div>
  );
};
